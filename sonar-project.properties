# SonarCloud / SonarQube — análisis de calidad en CI
# OBLIGATORIO: Sustituir sonar.organization y sonar.projectKey por los de tu proyecto en sonarcloud.io

# SonarCloud: crear proyecto en https://sonarcloud.io/projects/create y pegar aquí
sonar.projectKey=Nubestock_nubestock-dashboard
sonar.organization=nubestock

# Código fuente y exclusiones (archivos que NO se analizan)
sonar.sources=src
sonar.exclusions=**/node_modules/**,**/dist/**,**/__tests__/**,**/*.d.ts,**/types/**,**/vite-env.d.ts,**/examples/**,**/components/ui/**,**/main.tsx,**/contexts/**

# Cobertura (generada por npm test)
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Ignorar archivos de test: no analizar __tests__ (solo se analiza src)
sonar.test.exclusions=**/__tests__/**

# Excluir archivos de infraestructura/configuración del análisis de cobertura
# Estos archivos SE analizan para calidad de código, pero NO se mide su cobertura
sonar.coverage.exclusions=**/config/**,**/types/**,**/styles/**,**/*.d.ts,**/components/ui/**,**/vite-env.d.ts,**/contexts/**,**/hooks/**,**/utils/**,**/examples/**,**/main.tsx,src/app/contexts/*.tsx,src/app/config/*.ts
